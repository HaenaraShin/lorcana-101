/* 영어 카탈로그 — P1a(EN 저작)에서 채운다. 현재는 스텁(빈 객체).
 * 값이 없으면 t() 가 ko 로 폴백하므로, 채우기 전에는 EN 선택 시 한국어가 보인다.
 * 키 구조는 ko.js 와 동일해야 하며, test_i18n.js 의 패리티 검사로 강제한다. */
window.MESSAGES = window.MESSAGES || {};
window.MESSAGES.en = {};
